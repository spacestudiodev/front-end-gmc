import styled from 'styled-components'
import { Text } from './Text.style'

export const BNavIconCont = styled.div`

`

export const BNavTitle = styled(Text).attrs(() => {
    tertiary
})`

`

export const BNavDescription = styled(Text)`

`