import { DDownCont } from './Dropdown.style'

export default function Dropdown({ data = [], value = 0, style, className }) {
    return (
        <DDownCont style={style} className={className}>

        </DDownCont>
    )
}